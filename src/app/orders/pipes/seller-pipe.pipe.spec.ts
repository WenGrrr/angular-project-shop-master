import { SellerPipePipe } from './seller-pipe.pipe';

describe('SellerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SellerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
