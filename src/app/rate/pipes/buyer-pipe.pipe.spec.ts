import { BuyerPipePipe } from './buyer-pipe.pipe';

describe('BuyerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BuyerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
